<!DOCTYPE html>
<html>

<head>
    <title>Chỉnh sửa sản phẩm</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/4.6.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        /* CSS căn chỉnh */
        body {
            padding: 20px;
        }

        form {
            max-width: 400px;
            margin: 0 auto;
        }

        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }

        input[type="text"],
        input[type="number"] {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        button {
            padding: 8px 15px;
        }

        h1 {
            text-align: center;
        }
    </style>
</head>

<body>
    <h1>Chỉnh sửa sản phẩm</h1>

    <form method="POST" action="/editproduct/<%= product._id %>">
        <div class="form-row">
            <div class="form-group col-md-12">
                <label for="name">Tên sản phẩm:</label>
                <input type="text" id="name" name="name" value="<%= product.name %>" required>
            </div>

        </div>

        <div class="form-row">
            <div class="form-group col-md-6">
                <label for="originalPrice">Giá gốc:</label>
                <input type="number" id="originalPrice" name="originalPrice" value="<%= product.originalPrice %>"
                    required>
            </div>
            <div class="form-group col-md-6">
                <label for="discount">Giảm giá:</label>
                <input type="number" id="discount" name="discount" value="<%= product.discount %>" required>
            </div>
        </div>

        <div class="form-row">
            <div class="form-group col-md-6">
                <label for="category">Danh mục:</label>
                <input type="text" id="category" name="category" value="<%= product.category %>" required>
            </div>
            <div class="form-group col-md-6">
                <label for="image">Link hình ảnh:</label>
                <input type="text" id="image" name="image" value="<%= product.image %>" required>
            </div>
        </div>
        <div class="form-row">
            <div class="form-group col-md-6">
                <label for="weight">Trọng lượng:</label>
                <input type="text" id="weight" name="weight" value="<%= product.weight %>" required>
            </div>
            <div class="form-group col-md-6">
                <label for="capacity">Dung lượng:</label>
                <input type="text" id="capacity" name="capacity" value="<%= product.capacity %>" required>
            </div>
        </div>
        <div class="form-row">
            <div class="form-group col-md-6">
                <label for="high">Chiều cao:</label>
                <input type="text" id="high" name="high" value="<%= product.high %>" required>
            </div>
            <div class="form-group col-md-6">
                <label for="system">Hệ điều hành:</label>
                <input type="text" id="system" name="system" value="<%= product.system %>" required>
            </div>
        </div>
        <button type="submit" class="btn btn-primary"><i class="fas fa-save"></i> Lưu</button>
    </form>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/4.6.0/js/bootstrap.min.js"></script>
</body>

</html>